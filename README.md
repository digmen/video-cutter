# video-cutter

<img src="avatar.png" width="120" align="right">

Telegram-бот, который нарезает видео на своём сервере через ffmpeg. Присылаешь видео —
жмёшь кнопку: по минуте, на равные части, каждые N секунд или вырезать один кусок.

## Возможности

- Нарезка **без перекодирования** (`ffmpeg -c copy`) — быстро даже для многочасовых видео, сервер почти не грузится.
- Понятный UX: после загрузки видео — **кнопки**, подстроенные под длину ролика (короткому не предлагаются 5-минутные интервалы). Можно и текстом: `от 1:30 до 2:45`, `каждые 60`, `поминутно`, `на 5 частей`.
- **Большие файлы (до 2 ГБ)** через собственный [telegram-bot-api](https://github.com/tdlib/telegram-bot-api) (обычный Bot API ограничивает бота 20 МБ на приём / 50 МБ на отдачу).
- **Очередь** с обработкой по одной задаче и низким приоритетом (`nice`/`ionice`) — чтобы не мешать другим сервисам на том же сервере.
- Файлы (исходник и нарезки) **удаляются сразу** после отдачи.
- Опциональный вход по подписке на Telegram-канал, владелец без ограничений, телеметрия использования.

## Как устроено

| Файл | Что делает |
|---|---|
| `src/index.ts` | бот: приём видео, кнопки, запуск нарезки |
| `src/parse.ts` | разбор тайм-кодов и команд нарезки в отрезки |
| `src/keyboard.ts` | кнопки под конкретную длину видео |
| `src/ffmpeg.ts` | обёртки над ffmpeg/ffprobe |
| `src/queue.ts` | очередь задач (защита сервера) |
| `src/db.ts` | SQLite: задачи, события, вход |

## Запуск

Нужны Node 22+, ffmpeg и (для больших файлов) локальный `telegram-bot-api`:

```bash
docker run -d --name tg-bot-api --restart unless-stopped \
  -p 127.0.0.1:8082:8081 \
  -e TELEGRAM_API_ID=<api_id> -e TELEGRAM_API_HASH=<api_hash> -e TELEGRAM_LOCAL=1 \
  -v /path/botapi:/var/lib/telegram-bot-api \
  aiogram/telegram-bot-api:latest
```

`api_id`/`api_hash` берутся на [my.telegram.org](https://my.telegram.org).

```bash
cp .env.example .env   # заполнить BOT_TOKEN и пути
npm install
npm test
npm start
```

## Планы

- Пресеты для шортсов, вертикальный кроп 9:16, кадры-превью.
- «Умная» нарезка: пропуск тишины (`silencedetect`), поиск ярких моментов по громкости и смене сцен.
